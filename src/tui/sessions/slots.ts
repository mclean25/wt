/** TUI compatibility barrel; authoritative slot definitions live in core. */
export {
  DOTFILES_SLOT,
  MAIN_CLONE_SLOT,
  MANAGER_SLOT,
  OFFERED_SLOTS,
  SESSION_SLOTS,
  SLOT_SLUGS,
  WT_SOURCE_SLOT,
  dotfilesSlotAvailable,
  sessionSlot,
} from "../../core/session-slots.ts";
export type { SessionSlot } from "../../core/session-slots.ts";
